#!/usr/bin/env python

import sys


for line in sys.stdin:
    print line
    # data = line.split('\t', 1)
    # idx = data[0]
    # sum_vec = data[1].strip()
    # corpus_sums[int(idx)] = float(sum_vec)
    # print '{0}\t{1}'.format(str(idx), str(sum_vec))

